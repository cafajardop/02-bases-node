
console.log(process.env);

const {SHELL, HOMEBREW_PREFIX, npm_lifecycle_script} = process.env;

// console.log(SHELL);

const characters = ['Flash', 'Superman', 'Green Lintern', 'Batman'];

const [, , batman, prrohp ] = characters;

console.log(batman, prrohp);
